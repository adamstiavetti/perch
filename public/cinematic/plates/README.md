# Cinematic Globe Plates

These plate files are temporary local placeholders for the isolated `/lab/globe-plate-integration` route.

Expected paths:

- `globe-mobile.png`
- `globe-desktop.png`
- `globe-only.png`

Current status: all three files are copied from the same generated globe-only plate. They are not production-approved mobile and desktop exports.

Replace them with art-directed plates when available. Keep route arcs, aircraft, scanner, ticket, and CTA as separate layers so the final experience can remain interactive.
